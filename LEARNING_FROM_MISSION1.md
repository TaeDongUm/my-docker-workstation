1. vs code 이미지 미리보기 안됨.(command + shift + v)
- 깃허브 레포에 이미지가 있는 링크를 **[alt text](./images/photo.png)** 형태에 넣어야 함.

2. 쉡이란? zsh와 bash의 차이
- 쉘(Shell): 사용자가 입력한 명령어를 운영체제(커널)가 이해하도록 해석해 주는 프로그램
    - Bash 버전: 과거 맥OS 및 여러 리눅스에서 기본이던 전통적인 쉘로, 맥에서는 주로 구형 버전(3.2.x 등 라이선스 문제로 인해 오래된 버전)이 탑재되어 있었음
    - Zsh 버전: 최신 맥OS의 표준이 된 쉘로, 대화형 사용 환경과 강력한 확장 기능을 지원
- macOS는 macOS Catalina(10.15) 버전부터 기존 기본 쉘이었던 Bash 대신 Zsh(Z shell)를 기본 쉘로 채택하고 있다. 두 쉘은 명령어 구조가 유사하지만 편의성과 확장성에서 차이가 있음

3. Docker는 클라이언트(CLI)와 서버(Daemon)가 분리된 구조
- docker --version: 명령어 창구 역할을 하는 Docker CLI가 잘 설치되어 있는지 확인
- docker info: 백그라운드에서 진짜 일을 처리하는 Docker Daemon(엔진)이 정상적으로 동작 중인지 확인  
    - 여러 속성들이 뜨는데 속성값이 뜨면 백그라운드 데몬이 실행된다는 의미.

4. hello-world 컨테이너 실행
- Docker의 이미지 다운로드와 컨테이너 실행 과정을 확인한다.
- docker run --name hello-test hello-world
- 실행 후 상태를 확인한다.

```
 docker images
 docker ps
 docker ps -a
 docker logs hello-test 
```


- 이미지가 없으면 Docker가 자동으로 이미지를 다운로드함
- 컨테이너가 메시지를 출력한 뒤 종료됨
- 실행 중인 컨테이너는 docker ps에 나타나지 않음
- 종료된 컨테이너는 docker ps -a에서 확인 가능


AI와의 대화

docker 에서 바인드 마운트를 하든 볼륨으로 영속성을 수행하든 컨테이너 내부 파일과 외부 저장소를 마운트하면 양쪽에 데이터가 저장되는게 맞아?

아니면 마운트된 컨테이너 내부 폴더 위치는 단순히 외부 저장소를 바라볼 뿐이야?

아니요. **마운트하면 보통 “양쪽에 따로 저장”되는 게 아니라, 컨테이너 내부 경로가 외부 저장소를 그대로 바라보는 것**입니다. 즉 그 경로에 읽기/쓰기한 내용은 **실제로는 외부 저장소에 저장**되고, 컨테이너 내부에는 별도의 복사본이 생기는 개념이 아닙니다. Docker 문서도 볼륨을 컨테이너에 마운트하면 그 볼륨 디렉터리가 컨테이너 안에 연결된다고 설명합니다. [docs.docker](https://docs.docker.com/engine/storage/volumes/)

## 핵심 개념
- **컨테이너 내부 경로 = 연결된 외부 저장소의 접근점**입니다.
- 그래서 컨테이너에서 파일을 만들면 외부 저장소에 저장되고, 외부 저장소에서 바꾸면 컨테이너에서도 보입니다.
- 컨테이너 자체의 writable layer에 저장되는 게 아니라, 마운트 지점은 외부 저장소로 덮어씌워집니다. [docs.docker](https://docs.docker.com/engine/storage/volumes/)

## 바인드 마운트 vs 볼륨
- **바인드 마운트**는 호스트의 특정 디렉터리를 컨테이너 경로에 직접 연결합니다.
- **볼륨**은 Docker가 관리하는 저장 공간을 컨테이너에 연결합니다.
- 둘 다 “컨테이너 내부에 따로 복제해서 저장”하는 방식이 아니라, **연결된 외부 저장소를 컨테이너가 사용하는 방식**입니다. [docs.docker](https://docs.docker.com/engine/storage/volumes/)

## 예시
예를 들어 `-v /host/data:/app/data`로 마운트했다면:
- 컨테이너의 `/app/data`에 저장한 파일은 `/host/data`에 저장됩니다.
- `/app/data`는 단순한 내부 폴더가 아니라 `/host/data`를 가리키는 마운트 지점입니다.

원하시면 제가 다음으로 **“마운트된 경로와 컨테이너 레이어가 어떻게 우선순위로 동작하는지”**도 그림처럼 쉽게 설명해드릴게요.

writable layer가 뭐야? 도커 내부에는 writable layer가 존재하고 여기에 존재하는 폴더만 writable 하다는거야?
마운트 지점이라는게 컨테이너 내부 폴더 와 외부 저장소를 연결하는 지점을 의미하는거야? 즉, 컨테이너 내부라고도 할 수 있고 외부 저장소 폴더 위치라고도 할 수 있는건가?
컨테이너 내부 폴더가 /data라고 하면 도커는 이를 어떻게 관리하기에 /data폴더에 접근하면 외부 저장소가 보이는거야? mount layer가 따로 존재하고 여기에 /data 폴더 메타데이터 정보가 저장되나?

"컨테이너의 /app/data에 저장한 파일은 /host/data에 저장됩니다." 이 말은 도커에서 새로운 파일을 "저장하는 로직"이 수행되는게 아니라 "이관"의 느낌?의 과정을 수행한다고 봐야 하나?

볼륨은 도커 내부에 컨테이너와 독립된 메모리 공간을 차지하나?
도커 컨테이너가 갖는 폴더 구성과 볼륨이 갖는 폴더 구조가 다르나?

네, 개념을 정확히 잡으면 이렇게 이해하면 됩니다: **writable layer는 컨테이너의 기본 쓰기 영역**이고, **마운트된 경로는 그 writable layer를 “대신”하는 외부 저장소 연결점**입니다. 따라서 `/data`가 마운트돼 있으면 보통 `/data`에 대한 읽기/쓰기는 컨테이너의 writable layer가 아니라 마운트된 외부 저장소로 갑니다. Docker 볼륨은 Docker가 관리하는 지속 저장소이고, 볼륨/바인드 마운트 모두 컨테이너에 외부 저장소를 연결하는 방식입니다. [docs.docker](https://docs.docker.com/engine/storage/volumes/)

## writable layer란
컨테이너는 이미지의 읽기 전용 레이어들 위에 **컨테이너 전용 쓰기 레이어(writable layer)** 를 하나 얹어서 동작합니다.  
컨테이너 내부에서 마운트되지 않은 경로에 파일을 생성/수정하면 이 writable layer에 기록됩니다. 즉, “컨테이너 내부에서 쓸 수 있는 폴더만 따로 정해져 있다”기보다, **기본적으로는 컨테이너 파일시스템 전체가 쓰기 가능하지만, 읽기 전용 이미지 레이어 위의 변경분이 writable layer에 쌓인다**고 이해하는 게 맞습니다. [docs.docker](https://docs.docker.com/engine/storage/volumes/)

## 마운트 지점의 의미
마운트 지점은 **컨테이너 내부 경로와 외부 저장소가 만나는 연결 지점**입니다.  
그래서 `/data`는 “컨테이너 내부 폴더”처럼 보이지만, 마운트가 걸려 있으면 실제로는 그 경로가 외부 저장소를 가리키는 **표시점/접점**이 됩니다. 컨테이너 입장에서는 그냥 `/data`라는 경로로 보이고, 호스트 입장에서는 외부 디렉터리나 볼륨의 실제 위치를 가리킵니다. [docs.docker](https://docs.docker.com/engine/storage/volumes/)

## /data는 어떻게 보이냐
도커가 “/data 폴더를 따로 저장해서 보여준다”기보다는, 커널의 마운트 메커니즘으로 **그 경로의 파일시스템을 다른 저장소로 치환**합니다.  
그래서 `/data`에 접근하면 그 순간부터는 컨테이너 writable layer가 아니라 마운트된 저장소의 내용이 보입니다. 보통은 “마운트 메타데이터가 별도 레이어에 저장된다”기보다, **마운트 정보는 컨테이너의 설정과 커널의 mount namespace에 의해 관리된다**고 보는 편이 더 정확합니다. [docs.docker](https://docs.docker.com/engine/storage/volumes/)

## 저장 로직이냐, 이관이냐
말씀하신 표현으로는 **“이관”보다는 “직접 기록”**에 가깝습니다.  
컨테이너가 `/app/data/file.txt`에 쓰면 도커가 나중에 옮기는 게 아니라, 처음부터 그 마운트 지점의 실제 대상 저장소에 기록됩니다. 다만 컨테이너 안에서는 그게 내부 폴더에 저장되는 것처럼 보일 뿐입니다. [docs.docker](https://docs.docker.com/engine/storage/volumes/)

## 볼륨의 구조
볼륨은 **컨테이너와 독립된 저장소**이지만, 메모리 공간이 아니라 **디스크 기반 저장 공간**입니다. Docker가 보통 호스트의 `/var/lib/docker/volumes/...` 아래에서 관리하며, 컨테이너 내부 폴더 구조와는 별개입니다. 즉 **컨테이너의 폴더 구조**와 **볼륨의 실제 폴더 구조**는 다를 수 있고, 컨테이너에서는 마운트된 경로 하나로만 보입니다. [docs.docker](https://docs.docker.com/engine/storage/volumes/)

원하시면 다음 답변에서 **“컨테이너 파일시스템 / writable layer / mount / volume”를 그림처럼 층 구조로** 아주 직관적으로 정리해드릴게요.